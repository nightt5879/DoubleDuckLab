---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const paperEntries = await getCollection('papers');
const papers = paperEntries
  .map((e) => e.data)
  .sort((a, b) => b.year - a.year || a.title.localeCompare(b.title));
---
<BaseLayout title="Lab Website · 论文" lang="zh" pathPrefix="">
  <section class="page-head" data-reveal>
    <h1 class="page-title">论文</h1>
    <p class="page-subtitle">按年份排序的论文列表，强调信息可读性与层次。</p>
  </section>

  <div class="grid">
    {papers.map((p, i) => (
      <article class="card card-hover stack-sm" data-reveal data-reveal-delay={String(i * 80)}>
        <h3>{p.title}</h3>
        <p>{p.year} · {p.venue}</p>
      </article>
    ))}
  </div>
</BaseLayout>
